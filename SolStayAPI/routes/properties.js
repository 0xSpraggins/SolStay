module.exports = app => {
    const properties = require("../controllers/propertyController");
    const router = require("express").Router();

    //router.post("/", properties.create);
    //router.get("/", properties.getAll);
    //router.get("/:id", properties.getOne);
    //router.get("/:ownerId", properties.getPropertiesByUser);

}